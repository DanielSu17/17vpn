# hiddenCategory is used to hide specific categories in the response of `GET feed/celebrities/all`
# NOTE that its type is map which is more efficient to checking an element in a collection
hiddenCategory:
  fan_favorite_in_search:
