# enableTimeLimit control if we should limit the time of showing poke dialog
# timeLimit control how long the poke dialog will be showed to viewer in seconds
giftID: poke
enableTimeLimit: true
timeLimit: 300
