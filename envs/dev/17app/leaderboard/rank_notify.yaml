# rankThreshold: push a notification when rank < rankThreshold to rank >= rankThreshold (once a day)
# enableExlucde: enable `excludeInterval` or not
# excludeInterval: the time inverval that should not push notifications

rankThreshold: 20

enableExclude: true
excludeInterval: 00:00~00:30
