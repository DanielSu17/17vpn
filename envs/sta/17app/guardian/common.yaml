# enableMaster allow guardian to use high priority application sql
enableMaster: true # this will only apply after pod re deploy

cacheTTLSeconds: 86400 # this will only apply after pod re deploy

# timeoutSetting set the timeout of reading database
readMysqlTimeout: 3000 # millisecond, 0 for forever