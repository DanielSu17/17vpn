enable: true
tables:
  UserFollow:
    stage: 1
    probability: 16 # the number N indicated the probability (1/N) to trigger read data
  UserFollowRequested:
    stage: 1
    probability: 16 # the number N indicated the probability (1/N) to trigger read data
